<source>
  @type sample
  tag test_tag
</source>

<match *>
  @type http

  endpoint http://adapter:9000/api/logs/${tag}
  json_array true
  open_timeout 2

  <format>
    @type json
  </format>
  <buffer tag>
      flush_interval 10s
    </buffer>
</match>

<match *>
  @type stdout
</match>